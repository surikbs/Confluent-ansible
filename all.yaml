#- name: zookeeper Provisioning
#  hosts: zookeeper
#  gather_facts: no
#  tags:
#    - zookeeper
#  tasks:
#  - import_role:
#      name: confluent.zookeeper

- name: Server Setup
  hosts: all
  gather_facts: no
  become_user: "{{ confluent_user }}"
  become_method: su
  tags:
    - common
  tasks:
    - import_role:
        name: confluent.common
